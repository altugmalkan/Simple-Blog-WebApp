<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
        <h1>Posted Messages:</h1>
        <div class="posts-container">
            <% for(let i = 0; i < kisiler.length; i++) { %>
                <div class="post">
                    <h2><%= kisiler[i].name %> (<%= kisiler[i].email %>)</h2>
                    <p><%= kisiler[i].text %></p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>